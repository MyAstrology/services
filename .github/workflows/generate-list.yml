name: Generate Blog, Build & Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # full history needed

      # 2️⃣ Setup Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # 3️⃣ Install dependencies (✅ fixed)
      - name: Install dependencies
        run: npm install

      # 4️⃣ Generate blog list
      - name: Generate blog list
        run: node src/content/blog/generate-list.js

      # 5️⃣ Generate blog sitemap
      - name: Generate blog sitemap
        run: node src/content/blog/generate-sitemap.js

      # 6️⃣ Generate image sitemap
      - name: Generate image sitemap
        run: node src/content/blog/generate-image-sitemap.js

      # 7️⃣ Commit & push changes if any
      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          git add src/content/blog/list.json || true
          git add my-sitemap.xml || true
          git add image-sitemap.xml || true

          git diff --cached --quiet || git commit -m "🔄 Auto-update blog list & sitemaps"
          git pull --rebase origin main || true
          git push origin main

      # 8️⃣ Build project (✅ NEW)
      - name: Build project
        run: npm run build

      # 9️⃣ Upload artifact for GitHub Pages (✅ NEW)
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: github-pages
          path: ./dist  # ⚠️ যদি React (CRA) হয়, এটি ./build দিন
          if-no-files-found: error

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
